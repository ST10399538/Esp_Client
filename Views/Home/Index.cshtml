@model ESP_Client.Models.Allowance

@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>You have used @Model.count requests out of @Model.limit.</p>
</div>
